"""Package root for the Simu_IonSource codebase.

Use ``python -m src`` for the coupled CLI. Canonical implementation modules
live only under ``core``, ``env``, ``agents``, ``config``, ``data``, ``render``,
and ``utils``.
"""
